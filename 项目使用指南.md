# 深市主板指标筛选项目使用指南

## 1. 项目用途

本项目用于深市主板股票的技术指标筛选、单股技术分析、严格次日收益回测，以及每日滚动优化可调区间参数。行情数据来自公开接口，不需要 API 密钥。

项目包含三个浏览器入口：

- `szse_quant_app.py`：日常选股与指标筛选。
- `stock_analysis_app.py`：单只股票 K 线、MA、MACD、KDJ 图表分析。
- `strategy_backtest/backtest_app.py`：按与预测模块一致的条件进行交互式回测。

## 2. 目录说明

```text
IndicatorScreening_Asheres/
├─ pyproject.toml                         # 项目依赖声明
├─ uv.lock                                # 锁定后的可复现依赖版本
├─ 深交所数据.xlsx                        # 主板公司股票池
├─ fetch_szse_data.py                     # 更新股票池
├─ fetch_period_returns.py                # 获取逐日涨跌幅
├─ warm_szse_quant_cache.py               # 预测模块行情预热
├─ szse_quant_app.py                      # 预测页面
├─ stock_analysis.py                      # 单股分析计算核心
├─ stock_analysis_charts.py               # 单股分析图表核心
├─ stock_analysis_app.py                  # 单股分析页面
├─ strategy_backtest/
│  ├─ backtest_app.py                     # 交互式回测页面
│  ├─ backtest_core.py                    # 回测计算核心
│  ├─ factor_batch.py                     # 批量因子计算
│  ├─ run_backtest.py                     # 固定规则命令行回测
│  ├─ warm_factor_cache.py                # 回测因子预热
│  ├─ rolling_parameter_optimizer.py      # 每日滚动参数优化
│  ├─ szse_quant_app.py                   # 回测策略快照
│  └─ outputs/input_data/                 # 回测所需的严格次日收益输入
└─ .venv/                                 # 由 uv 自动创建，不应手工复制
```

运行过程中生成的 `data_cache/`、`strategy_backtest/data_cache/` 和 `strategy_backtest/outputs/rolling_parameter_updates/` 都是本机运行数据。缓存可删除后重新获取；每日滚动参数 JSON 建议保留。

## 3. 首次安装

### 前置条件

- Windows PowerShell
- Python 3.10 或更高版本
- `uv` 命令行工具

在本目录打开 PowerShell，执行：

```powershell
uv sync --locked
```

该命令会根据 `uv.lock` 创建 `.venv` 并安装项目所需的固定版本依赖。不要复制旧项目的 `.venv`，因为虚拟环境与本机 Python 路径相关。

确认环境可用：

```powershell
uv run --locked python --version
uv run --locked python -c "import pandas, streamlit; print(streamlit.__version__)"
```

## 4. 日常选股

首次使用或股票池需要更新时，先执行：

```powershell
uv run --locked python fetch_szse_data.py
```

启动预测页面：

```powershell
uv run --locked streamlit run szse_quant_app.py --server.port 8504
```

浏览器访问 `http://localhost:8504`。在页面中选择截至日期，点击“获取行情并计算因子”，再点击“开始筛选”。页面会按当前勾选的指标、风险过滤与参数区间筛选股票。

## 5. 单股技术分析

启动单股图表页：

```powershell
uv run --locked streamlit run stock_analysis_app.py --server.port 8507
```

浏览器访问 `http://localhost:8507`。可输入 `000001`、`000001.SZ`、`600000` 或 `SH600000` 等格式。页面展示 K 线、MA5、MA20、MACD 与可调 KDJ 参数的图表。

## 6. 获取严格次日收益数据

交互回测和滚动优化使用逐日涨跌幅文件中的严格相邻交易日收益。需要更新该输入时，执行：

```powershell
uv run --locked python fetch_period_returns.py --start-date 2026-05-01 --end-date 2026-07-28
```

生成的工作簿应放在 `strategy_backtest/outputs/input_data/`，或在回测、优化命令中通过 `--returns-workbook` 显式传入。首日会额外取开始日前的日线，确保区间内每个交易日都能计算涨跌幅。

## 7. 交互式回测

启动回测页面：

```powershell
uv run --locked streamlit run strategy_backtest/backtest_app.py --server.port 8501
```

浏览器访问 `http://localhost:8501`。页面的指标、风险过滤、默认参数与预测模块保持一致。回测只计算收益文件中存在严格次日真实涨跌幅的预测；没有信号或缺少严格次日收益的日期会单独统计为未预测。

首次完整回测可先预热因子缓存：

```powershell
uv run --locked python strategy_backtest/warm_factor_cache.py
```

## 8. 每日滚动参数优化

滚动优化固定当前默认的勾选项与风险过滤，只搜索已启用的可调参数：RSI、换手率、量比、涨幅、KDJ 金叉时间和 MACD 红蓝线区间。它只使用已有严格次日收益的实际交易日，避免使用尚未验证的当天信号。

默认向前优化 20 个有效交易日：

```powershell
uv run --locked python -m strategy_backtest.rolling_parameter_optimizer --lookback-days 20
```

首次运行以程序当前默认参数为起点。之后每天运行时，会自动读取当前日期之前最新的 `rolling_parameter_optimization_YYYY-MM-DD.json` 作为初始参数。

指定自定义起点或上一日参数：

```powershell
uv run --locked python -m strategy_backtest.rolling_parameter_optimizer `
  --lookback-days 20 `
  --starter-json strategy_backtest/outputs/rolling_parameter_updates/rolling_parameter_optimization_2026-07-28.json
```

可用 `--as-of-date YYYY-MM-DD` 重跑历史日期，用 `--minimum-prediction-days N` 限制最少有效预测天数。

## 9. 滚动优化 JSON 输出

每次运行会在 `strategy_backtest/outputs/rolling_parameter_updates/` 写入两份同日期 JSON：

- `rolling_parameter_optimization_YYYY-MM-DD.json`：供下一日继承的最佳参数、初始参数、固定筛选条件、数据窗口和优化摘要。
- `rolling_parameter_backtest_YYYY-MM-DD.json`：完整回测报告。`worksheets` 字段按原报告结构保存“优化汇总”“参数对比”“每日回测”“优化路径”“数据问题”的列名与逐行数据。

JSON 使用 UTF-8 编码、日期采用 `YYYY-MM-DD`、缺失值写为 `null`。回测报告不再生成 Excel 文件。

## 10. 缓存与问题排查

- `data_cache/`：预测页面与逐日收益脚本的行情缓存。
- `strategy_backtest/data_cache/`：回测长历史与全量因子缓存。
- 删除缓存不会删除源代码或股票池，但下次运行会重新联网下载，耗时更长。
- 股票日线获取失败、日线不足或严格次日收益缺失时，程序会记录问题并剔除对应股票，不会中断其他股票的计算。
- 如需强制重新拉取行情，可在页面启用“强制刷新”，或对命令行使用 `--force-refresh`。

## 11. 验证代码

安装完成后可运行全部自动化测试：

```powershell
uv run --locked python -m unittest discover -s tests -v
```

## 12. 常用端口

| 功能 | 端口 | 地址 |
| --- | ---: | --- |
| 预测筛选 | 8504 | `http://localhost:8504` |
| 交互回测 | 8501 | `http://localhost:8501` |
| 单股分析 | 8507 | `http://localhost:8507` |

同一端口已被占用时，修改命令中的 `--server.port` 即可。
