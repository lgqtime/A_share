# 任务：动态最优权重计算（基于滚动风险调整收益）

## 1. 输入数据
针对每天获取的数据完成下列任务

## 2. 核心目标
我们需要计算 **9天、21天、28天** 这三个特定时间窗口，并为它们**每日动态计算**最优持仓权重。
权重计算规则必须基于“风险调整后收益（滚动夏普评分）”，而非单纯比较收益率绝对值。

## 3. 计算逻辑
针对目标日期 **T日**，执行以下步骤以决定 **T+1日** 开盘时的持仓权重：

**步骤 A：提取滚动序列**
- 从数据中提取 **9天、21天、28天** 三个窗口在 **T日及此前连续4个交易日**（即最近5个交易日，含T日）的收益率数据。
- 形成三个长度为5的序列：R9 = [r9_1, r9_2, ..., r9_5]，R21 = [r21_1, ..., r21_5]，R28 = [r28_1, ..., r28_5]。

**步骤 B：计算统计指标**
- 对每个序列计算：
  - 平均值（Mean）：μ = (sum of 5 values) / 5
  - 样本标准差（Std Dev）：σ = sqrt( sum((xi - μ)^2) / (5 - 1) )  【注意：使用样本标准差，分母为 n-1】

**步骤 C：计算滚动夏普评分（Sharpe Score）**
- 公式：**S_i = μ_i / σ_i** （其中 i 代表 9、21、28）
- 特别规则：如果 σ_i = 0，则令 S_i = 0（防止除以零错误）。

**步骤 D：线性Softmax归一化（计算权重）**
- **重要**：此处使用线性比例归一化，**不是**指数Softmax（即不使用 exp(x)）。
- 公式：**W_i = S_i / (S_9 + S_21 + S_28)**
- 输出三个权重，确保 W_9 + W_21 + W_28 = 100%。

**步骤 E：分配至次日**
- 将计算得出的权重（W_9, W_21, W_28）分配给 **T+1日** 的持仓。

## 4. 重要约束与防未来函数
- **禁止使用未来数据**：计算 T 日权重时，绝对禁止使用 T+1 日及之后的收益率数据。
- **滚动窗口固定**：回溯期固定为最近 **5个交易日**，不可根据数据量动态调整。

## 5. 期望输出
请在原本的参数更新模块（包括定时任务对应的滚动每日参数和允许本地运行的文件）实现上述逻辑，最终将不同权重加权以后的最优参数保存到原来的位置
## 6. 数据加载提示
- 读取时请忽略可能存在的 BOM 头（如 UTF-8-BOM）。
- 数值均为浮点数。

## 7. 附加要求（代码风格）
- 代码要可复现，包含必要的库导入（pandas, numpy）。
- 计算标准差时务必指定 `ddof=1`（样本标准差）。

---

请根据以上提示词生成代码并确保能够运行。